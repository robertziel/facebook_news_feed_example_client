import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UpdatePostPage';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Update post',
  },
});
