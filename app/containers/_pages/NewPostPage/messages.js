import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NewPostPage';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'New post',
  },
});
